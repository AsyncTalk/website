---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<ul class="flex flex-wrap gap-2">
  {
    tags.map((x) => (
      <li class="rounded-md border border-white/10 bg-white/5 px-2 py-0.5 text-xs text-white/55">
        {x}
      </li>
    ))
  }
</ul>
